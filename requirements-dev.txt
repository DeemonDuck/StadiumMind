# Dev-only tooling (lint, type-check, tests) - NOT needed for the deployed
# app itself, so kept separate from requirements.txt (which Streamlit
# Community Cloud installs for the live deploy). Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1
ruff==0.15.21
mypy==2.2.0
